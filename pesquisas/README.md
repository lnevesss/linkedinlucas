# pesquisas/

Saída da **etapa 1** (subagent `pesquisador-wellness`). Cada execução gera um arquivo
`AAAA-MM-DD.md` com 5–8 notícias candidatas (título, fonte, link, data, resumo). A **etapa 2**
(`curador-noticias`) adiciona a seção "Notícia escolhida" no mesmo arquivo.

Gerado automaticamente — não precisa editar à mão.
